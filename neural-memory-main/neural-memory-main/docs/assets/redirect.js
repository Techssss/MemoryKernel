if (
  window.location.hostname === 'neuralmemory.theio.vn' &&
  (window.location.pathname === '/' || window.location.pathname === '/index.html' || window.location.pathname === '')
) {
  window.location.replace('/landing/pro-landing.html');
}
